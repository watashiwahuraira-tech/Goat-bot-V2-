module.exports = {
  config: {
    name: "murgi1g",
    version: "1.0.0",
    author: "Mahiru (Girl Version by Sajib)",
    countDown: 5,
    role: 2,
    category: "fun",
    guide: { en: "{pn} @mention" }
  },

  onStart: async function ({ api, event }) {

    const mention = Object.keys(event.mentions)[0];
    if (!mention)
      return api.sendMessage(
        "🫂 যেই মাইয়া রে চুদবি তাকে @mention কর 😘",
        event.threadID,
        event.messageID
      );

    const name = event.mentions[mention];
    const tag = [{ id: mention, tag: name }];

    function send(txt, delay = 0) {
      setTimeout(() => {
        api.sendMessage(txt, event.threadID);
      }, delay);
    }

    // 🔥 Gali roast converted to Girl target
    send(`তোর সজীব বাপ এর চোদা লো মাইয়া`, 0);

    send({
      body: `খাংকির মাইয়া তোর আম্মু রে চুদি 🥰 ${name}`,
      mentions: tag
    }, 3000);

    send({
      body: `খাংকির মাইয়া তোর কচি ভুদায় আমি সজীব চুদি 😍 ${name}`,
      mentions: tag
    }, 5000);

    send({
      body: `মাদারচোদ মাইয়া তোর আম্মুর পম পম খাই আমি তোর বাপ সজীব 🐰 ${name}`,
      mentions: tag
    }, 7000);

    send({
      body: `তোর কচি ভুদায় কামর দিমু আমি তোর আব্বু সজীব 💔 ${name}`,
      mentions: tag
    }, 9000);

    send({
      body: `খাংকি মাইয়া কথা কম, তোর আম্মু রে আইটেম বোম বানামু ${name}`,
      mentions: tag
    }, 12000);

    send({
      body: `depression থেকেও তোর মাইরে চু*** দি 🤬 ${name}`,
      mentions: tag
    }, 15000);

    send({
      body: `তোর আম্মু রে আচার এর লোভ দেখি চুদি 🤬 ${name}`,
      mentions: tag
    }, 17000);

    send({
      body: `তোর কচি ভুদায় থুতু দিয়ে দন ঢুকাই 🤟 ${name}`,
      mentions: tag
    }, 20000);

    send({
      body: `তোর আপুর কান্দে ফেলে চুদা দিমু 🤝 ${name}`,
      mentions: tag
    }, 23000);

    send({
      body: `উফফফ তোর আম্মুর কালা ভুদায় মাল আউট 😭 ${name}`,
      mentions: tag
    }, 25000);

    send({
      body: `অনলাইনের গালিবাজ মাইয়া তুই সজীব এর জারজ 😘 ${name}`,
      mentions: tag
    }, 28500);

    send({
      body: `তোর জামা খুলে দে দেখি ✋ ${name}`,
      mentions: tag
    }, 31000);

    send({
      body: `হাই মাদারচোদ মাইয়া তোর আম্মু রে আদর করে চুদি ${name}`,
      mentions: tag
    }, 36000);

    send("~ চুদা কি আরো খাবি মাগির মাইয়া 🤖", 39000);
  }
};
