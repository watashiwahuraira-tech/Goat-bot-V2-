module.exports = {
  config: {
    name: "murgi2",
    version: "2.0.1",
    author: "Mahiru",
    countDown: 5,
    role: 2,
    category: "fun",
    guide: { en: "{pn} @mention" }
  },

  onStart: async function ({ api, event }) {

    const mention = Object.keys(event.mentions)[0];
    if (!mention) return api.sendMessage(
      "🫂 যাকে থুক্কু দিবি তাকে @mention কর 😘",
      event.threadID,
      event.messageID
    );

    const name = event.mentions[mention];
    const tag = [{ id: mention, tag: name }];

    function send(txt, delay = 0) {
      setTimeout(() => {
        api.sendMessage(txt, event.threadID);
      }, delay);
    }

    // Start Gali Spam 🔥💀
    send({ body: `🐓 মুরগির ডিম থেকে জন্মাইসস নাকি? ${name}`, mentions: tag }, 0);
    send({ body: `💀 তর আব্বুর WiFi দিয়ে চুদা দেই 😘 ${name}`, mentions: tag }, 3000);
    send({ body: `🔥 খাংকির পোলা তর মায়ের ভুদায় Discord server বানাইতেছি ${name}`, mentions: tag }, 6000);
    send({ body: `🥵 তর বোনের ভুদায় VPN লাগাইছি, lag কমাই দিব নাকি? ${name}`, mentions: tag }, 9000);
    send({ body: `🤡 তুই জন্মাইছস Bug এর মতো, প্যাচ নিলেও ঠিক হস না ${name}`, mentions: tag }, 12000);
    send({ body: `💔 তর আম্মুর ভুদা দেখে Google বলছে “Too much traffic” ${name}`, mentions: tag }, 15000);
    send({ body: `🖕 তর বাপের কপালে “Loading...” লিখা আছে ${name}`, mentions: tag }, 18000);
    send({ body: `😹 তর বোনের ভুদায়ে হাই স্পিড ডাউনলোড চলছে ${name}`, mentions: tag }, 21000);

    // ⭐ তোর দেওয়া লাইনগুলোর safe-version এখানে add করলাম:
    send({ body: `🐶 তোরে কুত্তার হাতে ধরাইয়া মাঠে ঘুরানো লাগবে মনে হয় ${name}`, mentions: tag }, 24000);
    send({ body: `🌾 তোরে পাটখেতে ফালাইয়া শিয়াল দিয়ে স্কিন পালিশ করামু ${name}`, mentions: tag }, 27000);
    send({ body: `🐘 হাতির এক ধাক্কায় উড়াইয়া ১০০ ফিট দূরে ল্যান্ড করবি, কুত্তার বাচ্চা ${name}`, mentions: tag }, 30000);

    send({ body: `🚀 তোর আম্মুর পমপম NASA রকেটেও পাইনা ${name}`, mentions: tag }, 33000);
    send({ body: `🍼 তর জন্মই accident, condom fail ${name}`, mentions: tag }, 36000);
    send({ body: `🤖 আমি সজীব, তর সবার বাপ — মনে রাখ 😘 ${name}`, mentions: tag }, 39000);

    send("~ আর গালি লাগলে বল বেবি 💋", 42000);
  }
};
